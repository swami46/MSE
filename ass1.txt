protocol:

* By default, all methods declared in a protocol are required methods.
* Protocols inherit from other protocols.
* Protocols are also useful in situations where the class of an object
isn’t known, or needs to stay hidden.

Subclass:

A subclass can override a method it inherits from the class it is
based on.The class it is based on is called its
   superclass